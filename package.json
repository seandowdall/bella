{
  "name": "bella",
  "private": true,
  "type": "module",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "build:sdks": "bun run --filter '@bella/*' build",
    "typecheck:sdks": "bun run --filter '@bella/*' typecheck"
  },
  "overrides": {
    "postcss": "8.5.15"
  },
  "devDependencies": {
    "typescript": "~6.0.2"
  }
}
