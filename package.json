{
  "name": "bella",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*"],
  "scripts": {
    "build:sdks": "bun run --filter '@bella/*' build",
    "typecheck:sdks": "bun run --filter '@bella/*' typecheck"
  },
  "devDependencies": {
    "typescript": "~6.0.2"
  }
}
